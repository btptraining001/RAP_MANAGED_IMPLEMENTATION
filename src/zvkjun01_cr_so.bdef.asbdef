projection;
strict ( 2 );
use draft;

use side effects;
define behavior for ZVKJUN01_CR_SO //alias <alias_name>

use etag
{
  use create;
  use update;
  use delete;

  use association _Item { create; }

  use action updateaction;

  use action Edit;
  use action Prepare;
  use action Activate;
  use action Discard;
  use action Resume;
}

define behavior for ZVKJUN01_CI_SOIT //alias <alias_name>
{
  use update;
  use delete;

  use association _Header;
}